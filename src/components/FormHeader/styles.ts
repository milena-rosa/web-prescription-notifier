import styled from 'styled-components'

export const Container = styled.div`
  align-items: center;
  background: var(--green-primary-30);
  color: var(--black);
  display: flex;
  filter: drop-shadow(0px 5px 8px rgba(0, 0, 0, 0.56));
  font-size: 2rem;
  font-weight: bold;
  height: 3.75rem;
  justify-content: center;
  line-height: 1.75rem;
  margin: 4.375rem 2rem 0;
  text-transform: uppercase;
`
